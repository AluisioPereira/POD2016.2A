-- postgresql
CREATE TABLE pessoa(
    id SERIAL NOT NULL, 
    nome VARCHAR(100) NOT NULL,
    PRIMARY KEY(id)
);

--my sql
CREATE TABLE pessoa (
     id MEDIUMINT NOT NULL AUTO_INCREMENT,
     nome VARCHAR(100) NOT NULL,
     PRIMARY KEY(id)
);


